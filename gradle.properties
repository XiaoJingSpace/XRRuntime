# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# org.gradle.java.home=C:\\Path\\To\\Java17
# Uncomment and set the path above if you want to specify a specific Java version
# Otherwise, Gradle will use the JAVA_HOME environment variable or system default
android.useAndroidX=true
android.enableJetifier=true

# Note: Do not set android.injected.* properties with empty values
# These are only set by IDE when importing projects
# Removing them allows command-line builds to work properly

