#ifndef JNI_BRIDGE_H
#define JNI_BRIDGE_H

#include <jni.h>
#include "openxr/openxr_api.h"

// JNI bridge functions - these are defined in openxr_api.cpp
// The functions are declared in openxr_api.h, so we just include that header
// No need to redeclare them here to avoid duplicate symbols

#endif // JNI_BRIDGE_H

